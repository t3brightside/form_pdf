# Non composer Lib

All composer dependencies should be placed here.
The extension would autoload it by itself.

Possible structure of this folder:

```
+--PHP
   +--composer
   +--mpdf
   +--myclabs
   +--paragonie
   +--psr
   +--setasign
   +--.gitkeep
   +--autoload.php
   +--README.md
```
